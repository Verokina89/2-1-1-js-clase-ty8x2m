// Declara tres variables utilizando let o const con diferentes tipos de datos: un número, una cadena de caracteres y un booleano.
let numero = 5
const texto = 'esto es un texto';
let boolean = true;

// Muestra en la consola el contenido de cada variable utilizando console.log().
console.log('variable numero:', numero);
console.log('variable texto', texto);
consolo.log('variable truefalse' trufalse

// Intenta hacer cambios en cada una de las variables con otros valores y tipos.
numero = 8;
console.log('variable numero:' numero);
